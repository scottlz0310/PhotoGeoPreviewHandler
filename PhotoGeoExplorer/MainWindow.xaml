<Window
    x:Class="PhotoGeoExplorer.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mapsui="using:Mapsui.UI.WinUI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:primitives="using:Microsoft.UI.Xaml.Controls.Primitives"
    mc:Ignorable="d"
    Title="PhotoGeoExplorer">
    <Grid x:Name="RootGrid">
        <Grid.Resources>
            <DataTemplate x:Key="IconItemTemplate">
                <StackPanel Width="140" Margin="0,0,8,8" Spacing="6">
                    <Border
                        Width="120"
                        Height="120"
                        CornerRadius="8"
                        Background="{ThemeResource SystemControlBackgroundBaseLowBrush}">
                        <Grid>
                            <Image
                                Source="{Binding Thumbnail}"
                                Stretch="UniformToFill"
                                Visibility="{Binding ThumbnailVisibility}" />
                            <FontIcon
                                Glyph="&#xE8B7;"
                                FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"
                                FontSize="28"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="{Binding FolderIconVisibility}" />
                        </Grid>
                    </Border>
                    <TextBlock Text="{Binding FileName}" TextWrapping="Wrap" />
                </StackPanel>
            </DataTemplate>
            <DataTemplate x:Key="ListItemTemplate">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,4" Spacing="8">
                    <Border
                        Width="40"
                        Height="40"
                        CornerRadius="6"
                        Background="{ThemeResource SystemControlBackgroundBaseLowBrush}">
                        <Grid>
                            <Image
                                Source="{Binding Thumbnail}"
                                Stretch="UniformToFill"
                                Visibility="{Binding ThumbnailVisibility}" />
                            <FontIcon
                                Glyph="&#xE8B7;"
                                FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"
                                FontSize="20"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="{Binding FolderIconVisibility}" />
                        </Grid>
                    </Border>
                    <TextBlock Text="{Binding FileName}" VerticalAlignment="Center" />
                </StackPanel>
            </DataTemplate>
            <DataTemplate x:Key="DetailsItemTemplate">
                <Grid Margin="0,0,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Border
                        Width="56"
                        Height="56"
                        CornerRadius="6"
                        Background="{ThemeResource SystemControlBackgroundBaseLowBrush}">
                        <Grid>
                            <Image
                                Source="{Binding Thumbnail}"
                                Stretch="UniformToFill"
                                Visibility="{Binding ThumbnailVisibility}" />
                            <FontIcon
                                Glyph="&#xE8B7;"
                                FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"
                                FontSize="24"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="{Binding FolderIconVisibility}" />
                        </Grid>
                    </Border>
                    <TextBlock
                        Grid.Column="1"
                        Text="{Binding FileName}"
                        TextTrimming="CharacterEllipsis"
                        VerticalAlignment="Center" />
                    <TextBlock
                        Grid.Column="2"
                        Text="{Binding ModifiedAtText}"
                        Opacity="0.7"
                        VerticalAlignment="Center" />
                    <TextBlock
                        Grid.Column="3"
                        Text="{Binding SizeText}"
                        Opacity="0.7"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center" />
                </Grid>
            </DataTemplate>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <MenuBar>
            <MenuBarItem Title="File" />
            <MenuBarItem Title="View" />
            <MenuBarItem Title="Settings" />
            <MenuBarItem Title="Help" />
        </MenuBar>

        <Grid x:Name="MainContentGrid" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="FileBrowserColumn" Width="2*" />
                <ColumnDefinition x:Name="SplitterColumn" Width="Auto" />
                <ColumnDefinition x:Name="DetailColumn" Width="3*" />
            </Grid.ColumnDefinitions>

            <Grid x:Name="FileBrowserPane" Grid.Column="0" Padding="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="File Browser" FontSize="20" FontWeight="SemiBold" />
                <CommandBar Grid.Row="1" Margin="0,8,0,0">
                    <AppBarButton Icon="Back" ToolTipService.ToolTip="Back" IsEnabled="False" />
                    <AppBarButton Icon="Forward" ToolTipService.ToolTip="Forward" IsEnabled="False" />
                    <AppBarButton Icon="Up" ToolTipService.ToolTip="Up" Click="OnNavigateUpClicked" />
                    <AppBarButton Icon="Refresh" ToolTipService.ToolTip="Refresh" Click="OnRefreshClicked" />
                    <AppBarButton Icon="Home" ToolTipService.ToolTip="Home" Click="OnNavigateHomeClicked" />
                </CommandBar>
                <BreadcrumbBar
                    Grid.Row="2"
                    Margin="0,8,0,0"
                    ItemsSource="{Binding BreadcrumbItems}"
                    ItemClicked="OnBreadcrumbItemClicked">
                    <BreadcrumbBar.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <TextBlock Text="{Binding Title}" />
                                <Button
                                    Content="v"
                                    Padding="4,0"
                                    MinWidth="24"
                                    IsEnabled="{Binding HasChildren}"
                                    Tag="{Binding}"
                                    Click="OnBreadcrumbDropDownClicked" />
                            </StackPanel>
                        </DataTemplate>
                    </BreadcrumbBar.ItemTemplate>
                </BreadcrumbBar>
                <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="8" Margin="0,8,0,0">
                    <TextBox
                        PlaceholderText="Search files..."
                        Width="240"
                        Text="{Binding SearchText, Mode=TwoWay}"
                        KeyDown="OnSearchKeyDown" />
                    <Button Content="Filters" Click="OnApplyFiltersClicked" />
                    <Button Content="Open" Click="OnOpenFolderClicked" />
                    <ComboBox
                        MinWidth="120"
                        SelectedIndex="{Binding FileViewModeIndex, Mode=TwoWay}"
                        ToolTipService.ToolTip="View mode">
                        <ComboBoxItem Content="Icon" />
                        <ComboBoxItem Content="List" />
                        <ComboBoxItem Content="Details" />
                    </ComboBox>
                </StackPanel>
                <CheckBox
                    Grid.Row="4"
                    Content="Show images only"
                    Margin="0,8,0,0"
                    IsChecked="{Binding ShowImagesOnly, Mode=TwoWay}"
                    Checked="OnFiltersChanged"
                    Unchecked="OnFiltersChanged" />
                <Grid Grid.Row="5" Margin="0,8,0,0">
                    <ListView
                        ItemsSource="{Binding Items}"
                        SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                        SelectionMode="Single"
                        IsItemClickEnabled="True"
                        ItemClick="OnFileItemClicked"
                        ItemTemplate="{StaticResource ListItemTemplate}"
                        Visibility="{Binding ListViewVisibility}" />
                    <GridView
                        ItemsSource="{Binding Items}"
                        SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                        SelectionMode="Single"
                        IsItemClickEnabled="True"
                        ItemClick="OnFileItemClicked"
                        ItemTemplate="{StaticResource IconItemTemplate}"
                        Visibility="{Binding IconViewVisibility}" />
                    <ListView
                        ItemsSource="{Binding Items}"
                        SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                        SelectionMode="Single"
                        IsItemClickEnabled="True"
                        ItemClick="OnFileItemClicked"
                        ItemTemplate="{StaticResource DetailsItemTemplate}"
                        HorizontalContentAlignment="Stretch"
                        Visibility="{Binding DetailsViewVisibility}" />
                    <Border
                        Padding="16"
                        CornerRadius="8"
                        Background="{ThemeResource SystemControlBackgroundBaseLowBrush}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Visibility="{Binding StatusVisibility}">
                        <StackPanel Spacing="8" MaxWidth="320" HorizontalAlignment="Center">
                            <SymbolIcon
                                Symbol="{Binding StatusSymbol}"
                                Width="28"
                                Height="28"
                                HorizontalAlignment="Center" />
                            <TextBlock
                                Text="{Binding StatusTitle}"
                                FontSize="16"
                                FontWeight="SemiBold"
                                TextAlignment="Center"
                                TextWrapping="Wrap" />
                            <TextBlock
                                Text="{Binding StatusDetail}"
                                Opacity="0.7"
                                TextAlignment="Center"
                                TextWrapping="Wrap" />
                            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                                <Button
                                    Content="{Binding StatusPrimaryActionLabel}"
                                    Visibility="{Binding StatusPrimaryActionVisibility}"
                                    Click="OnStatusPrimaryActionClicked" />
                                <Button
                                    Content="{Binding StatusSecondaryActionLabel}"
                                    Visibility="{Binding StatusSecondaryActionVisibility}"
                                    Click="OnStatusSecondaryActionClicked" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>

            <primitives:Thumb
                x:Name="MainSplitter"
                Grid.Column="1"
                Width="6"
                Background="{ThemeResource SystemControlBackgroundBaseLowBrush}"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                DragDelta="OnMainSplitterDragDelta"
                IsTabStop="False" />

            <Grid x:Name="DetailPane" Grid.Column="2" Padding="12">
                <Grid.RowDefinitions>
                    <RowDefinition x:Name="PreviewRow" Height="*" MinHeight="200" />
                    <RowDefinition x:Name="MapSplitterRow" Height="Auto" />
                    <RowDefinition x:Name="MapRow" Height="*" MinHeight="200" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Spacing="2">
                            <TextBlock Text="Image Preview" FontSize="20" FontWeight="SemiBold" />
                            <TextBlock
                                Text="{Binding MetadataSummary}"
                                Visibility="{Binding MetadataVisibility}"
                                Opacity="0.7"
                                FontSize="12" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <Button Click="OnPreviewFitClicked" ToolTipService.ToolTip="ウィンドウに合わせる">
                                <SymbolIcon Symbol="View" />
                            </Button>
                            <Button Click="OnPreviewZoomInClicked" ToolTipService.ToolTip="拡大">
                                <SymbolIcon Symbol="ZoomIn" />
                            </Button>
                            <Button Click="OnPreviewZoomOutClicked" ToolTipService.ToolTip="縮小">
                                <SymbolIcon Symbol="ZoomOut" />
                            </Button>
                            <ToggleButton
                                Checked="OnPreviewMaximizeChecked"
                                Unchecked="OnPreviewMaximizeUnchecked"
                                ToolTipService.ToolTip="最大化">
                                <SymbolIcon Symbol="FullScreen" />
                            </ToggleButton>
                            <Button Click="OnPreviewPreviousClicked" ToolTipService.ToolTip="前の画像">
                                <SymbolIcon Symbol="Back" />
                            </Button>
                            <Button Click="OnPreviewNextClicked" ToolTipService.ToolTip="次の画像">
                                <SymbolIcon Symbol="Forward" />
                            </Button>
                        </StackPanel>
                    </Grid>
                    <Border
                        Grid.Row="1"
                        Margin="0,8,0,0"
                        Background="{ThemeResource SystemControlBackgroundBaseLowBrush}"
                        CornerRadius="6">
                        <Grid>
                            <ScrollViewer
                                x:Name="PreviewScrollViewer"
                                ZoomMode="Enabled"
                                MinZoomFactor="0.1"
                                MaxZoomFactor="6"
                                HorizontalScrollMode="Disabled"
                                VerticalScrollMode="Disabled"
                                IsScrollInertiaEnabled="False"
                                HorizontalScrollBarVisibility="Auto"
                                VerticalScrollBarVisibility="Auto"
                                SizeChanged="OnPreviewScrollViewerSizeChanged"
                                PointerWheelChanged="OnPreviewPointerWheelChanged"
                                PointerPressed="OnPreviewPointerPressed"
                                PointerMoved="OnPreviewPointerMoved"
                                PointerReleased="OnPreviewPointerReleased"
                                PointerCaptureLost="OnPreviewPointerCaptureLost">
                                <Image
                                    x:Name="PreviewImage"
                                    Source="{Binding SelectedPreview}"
                                    Stretch="Uniform"
                                    ImageOpened="OnPreviewImageOpened" />
                            </ScrollViewer>
                            <TextBlock
                                Text="Select a photo to preview."
                                Visibility="{Binding PreviewPlaceholderVisibility}"
                                Foreground="{ThemeResource SystemFillColorCautionBrush}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                IsHitTestVisible="False"
                                TextWrapping="Wrap" />
                        </Grid>
                    </Border>
                </Grid>

                <primitives:Thumb
                    x:Name="MapRowSplitter"
                    Grid.Row="1"
                    Height="6"
                    Background="{ThemeResource SystemControlBackgroundBaseLowBrush}"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Center"
                    DragDelta="OnMapSplitterDragDelta"
                    IsTabStop="False" />

                <Grid x:Name="MapPane" Grid.Row="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="Map" FontSize="20" FontWeight="SemiBold" />
                    <Grid Grid.Row="1" Margin="0,8,0,0">
                        <Border Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" CornerRadius="6" />
                        <Grid x:Name="MapHost">
                            <mapsui:MapControl x:Name="MapControl" />
                        </Grid>
                        <Border
                            x:Name="MapStatusOverlay"
                            Background="#D0000000"
                            Visibility="Collapsed"
                            IsHitTestVisible="False" />
                        <Border
                            x:Name="MapStatusPanel"
                            Padding="16"
                            CornerRadius="8"
                            Background="{ThemeResource SystemControlBackgroundBaseLowBrush}"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="Collapsed"
                            IsHitTestVisible="False">
                            <StackPanel Spacing="8" MaxWidth="320" HorizontalAlignment="Center">
                                <SymbolIcon
                                    x:Name="MapStatusIcon"
                                    Symbol="Map"
                                    Width="28"
                                    Height="28"
                                    HorizontalAlignment="Center" />
                                <TextBlock
                                    x:Name="MapStatusTitle"
                                    FontSize="16"
                                    FontWeight="SemiBold"
                                    TextAlignment="Center"
                                    TextWrapping="Wrap" />
                                <TextBlock
                                    x:Name="MapStatusDescription"
                                    Opacity="0.7"
                                    TextAlignment="Center"
                                    TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>

        <Grid Grid.Row="2" Padding="12,6,12,8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <controls:InfoBar
                Grid.Row="0"
                IsOpen="{Binding IsNotificationOpen, Mode=TwoWay}"
                Message="{Binding NotificationMessage}"
                Severity="{Binding NotificationSeverity}"
                IsClosable="True" />
            <Border
                Grid.Row="1"
                Margin="0,6,0,0"
                Padding="8,4"
                Background="{ThemeResource SystemControlBackgroundBaseLowBrush}"
                CornerRadius="4">
                <TextBlock
                    Text="{Binding StatusBarText}"
                    TextTrimming="CharacterEllipsis"
                    VerticalAlignment="Center" />
            </Border>
        </Grid>
    </Grid>
</Window>

